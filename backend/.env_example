# PostgreSQL connection (SQLAlchemy-style URL; empty password → omit :password before @)
DATABASE_URL=postgresql://postgres@localhost:PORT/DB_NAME

# Comma-separated frontend origins allowed by CORS
CORS_ORIGINS=http://127.0.0.1:PORT,http://localhost:PORT
