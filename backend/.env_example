# PostgreSQL connection (SQLAlchemy-style URL; empty password → omit :password before @)
DATABASE_URL=postgresql://postgres@localhost:5432/loan_tracker

# Optional (add when you wire the app)
# APP_ENV=development
